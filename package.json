{
  "name": "barak-meetings",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend/src/BookingPlatform.API && dotnet watch run --urls http://localhost:5001",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "cd backend/src/BookingPlatform.API && dotnet build -c Release",
    "build:frontend": "cd frontend && npm run build",
    "install:all": "cd frontend && npm install",
    "db:migrate": "cd backend/src/BookingPlatform.API && dotnet ef database update",
    "db:migration:add": "cd backend/src/BookingPlatform.API && dotnet ef migrations add"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
